name: Boost socks5 THIRD 50k

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

jobs:
  boost:
    uses: asifshaikhtsn/Walla/.github/workflows/geo-proxy-run.yml@main
    with:
      script: boost_aggregator.py
      dependencies: "aiohttp aiohttp-socks"
      commit_message: "Boost: update proxy lists [skip ci]"
      timeout_minutes: 120
    secrets: inherit
